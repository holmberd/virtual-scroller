{
  "name": "build-dev",
  "version": "1.0.0",
  "description": "",
  "author": "Dag Holmberg (https://github.com/holmberd/)",
  "license": "BSD-3-Clause",
  "main": "index.js",
  "devDependencies": {
    "esbuild": "^0.14.25"
  },
  "dependencies": {
    "lit-html": "^2.2.1",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "scripts": {
    "build": "esbuild --bundle ./index.js --loader:.js=jsx --outfile=build/index.js",
    "start": "npm run build -- --servedir=build",
    "watch": "npm run build -- --watch"
  }
}
