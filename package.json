{
  "name": "view-element",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "jest",
    "build": "esbuild --bundle src/index.js --outfile=build/index.js",
    "start": "npm run build -- --servedir=build",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "construct-style-sheets-polyfill": "^3.1.0",
    "esbuild": "^0.14.25",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "lit-html": "^2.2.1"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/src/tests/setup-tests.js",
      "construct-style-sheets-polyfill"
    ],
    "transform": {
      "^.+\\.js?$": "esbuild-jest"
    }
  }
}
